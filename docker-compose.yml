services:
  minecraft-map:
    image: nginx:alpine
    container_name: minecraft-navigation
    ports:
      - "${APP_PORT:-8080}:80"
    volumes:
      - ./app:/usr/share/nginx/html:ro
    restart: unless-stopped
